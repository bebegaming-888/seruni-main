{
  "buildCommand": "npm install --include=dev && npx vite build && node scripts/postbuild.js",
  "outputDirectory": "dist/client",
  "installCommand": "npm install --include=dev",
  "framework": null
}
