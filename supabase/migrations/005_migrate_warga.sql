-- ============================================================
-- MIGRATION 005: Seed warga from Tabel_Penduduk.csv
-- ============================================================
-- Source : Tabel_Penduduk.csv (7.867 rows)
-- Layout : Main file (this) + 79 sequential seed files in ./warga-seeds/
-- Order  : Seed files use prefix 005.001 → 005.079 so they run
--          in the correct sequence via Supabase CLI / pg CLI
-- Size   : Each seed file ~64KB (100 rows) — safe for SQL Editor
--
-- How to run:
--   CLI (recommended — runs all in order):
--     for f in supabase/migrations/warga-seeds/005.*_warga_seed.sql; do
--       psql "$DATABASE_URL" -f "$f"; done
--
--   Supabase CLI:
--     supabase db push  (CLI picks up numbered files automatically)
--
--   SQL Editor (one by one):
--     Open warga-seeds/005.001_warga_seed.sql → Run
--     Open warga-seeds/005.002_warga_seed.sql → Run
--     ... until 079
--
-- Idempotent: ON CONFLICT (nik) DO UPDATE SET updated_at = now()
-- Safe to re-run if interrupted.
-- ============================================================

-- No data in this file — see ./warga-seeds/
select 'Run all files in warga-seeds/ in order: 005.001 → 005.079' as note;
